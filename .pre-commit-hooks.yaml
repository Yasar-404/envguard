- id: envguard
  name: envguard
  description: Block commits that add secrets or credentials.
  entry: envguard scan --staged
  language: python
  pass_filenames: false
  stages: [pre-commit]
  minimum_pre_commit_version: "3.2.0"
